// definindo a funcao
function ajuste(width) {
  const pi = 3.14159265
  return (width*2*pi + 4.15)
}

function sum(a, b) {
  return a + b
}

const subtraction = function (a, b) {
  return a - b
}

const multiply = (a, b) => a * b


console.log(subtraction)
radius = ajuste(width = 3)
ajuste(width = 4)
ajuste(width = 5)